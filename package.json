{
  "name": "resort-calendar-scheduler",
  "version": "1.0.0",
  "description": "MERN Stack Resort Booking Calendar Scheduler",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
